{
  "name": "@townshipcanada/google-sheets-addon",
  "version": "1.0.0",
  "private": true,
  "description": "Google Sheets Add-On for converting Canadian legal land descriptions to GPS coordinates",
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "deploy": "clasp deploy",
    "logs": "clasp logs",
    "watch": "clasp push --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
